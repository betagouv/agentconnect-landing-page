# AgentConnect - site vitrine

Site vitrine du service AgentConnect.

## Démarrage rapide

```
npm install
cp .env.example .env
docker-compose up
```

Ou la version sans Docker :

```shell
npm install
cp .env.example .env
npm i
npm start
```
